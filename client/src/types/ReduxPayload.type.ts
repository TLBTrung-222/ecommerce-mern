export interface UserActionPayload {
    name: string
    email: string
    phone: string
    avatar: string
    access_token: string
    id: number
    is_admin: boolean
    address: string
    city: string
}
